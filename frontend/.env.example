# Frontend deployment path. Use "/" when hosted at the domain root.
VITE_BASE_PATH=/wbs/

# API base URL used by axios. Relative paths are resolved from the current host.
# Examples:
# VITE_API_BASE_URL=/wbs/api/
# VITE_API_BASE_URL=http://localhost:8000/api/
VITE_API_BASE_URL=/wbs/api/
